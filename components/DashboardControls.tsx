import React from 'react';

// This component is now largely superseded by the DataDiscoveryPanel and direct map interaction.
// We keep it minimal or could remove it, but for now, we'll render nothing to clean up the UI
// while preserving the file if user wants to revert.
// The controls are now embedded in the App.tsx overlay.

const DashboardControls: React.FC<any> = () => {
  return null;
};

export default DashboardControls;